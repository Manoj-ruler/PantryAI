1. The "Killer" Project Concept: PantryAIThe Elevator Pitch:"PantryAI is an AI-native 'Kitchen Operating System' that converts digital content (recipes/reels) into instant Instamart carts and uses predictive analytics to automate household restocking, ensuring zero-friction commerce for busy urban households."2. The Application Blueprint (Fill these in the Form)A. Use Case Description"I am building PantryAI, a multi-platform AI agent (WhatsApp & Web) that solves two primary pain points in Q-Commerce:Intent-to-Action Gap: Users see recipes on social media but struggle to find all 10+ ingredients on Instamart. PantryAI parses recipe text/video and uses Swiggy’s Search & Cart APIs to build a complete, localized cart in one click.Predictive Replenishment: By analyzing user order frequency for staples (milk, bread, eggs), the agent proactively prompts the user for a 'One-Tap Restock' via the Checkout API before they run out."B. How will you use Swiggy APIs? (Technical Logic)Search API: To map extracted recipe ingredients to the highest-rated, available SKUs in the user's nearest Instamart dark store.Cart API: To programmatically bundle multiple items, handling edge cases like "out of stock" by suggesting the best alternatives.Order History API: To feed a lightweight machine-learning model that predicts the next "Run-out Date" for recurring household items.MCP Servers: I intend to use the Swiggy MCP to standardize the communication between my LLM (running on AWS Bedrock) and Swiggy’s real-time inventory.C. Expected Impact & Scale"PantryAI targets high-frequency Swiggy users. By moving the 'Order Trigger' from the Swiggy App to where the user spends their time (WhatsApp/Social Media), we expect to:Increase Average Order Value (AOV) by bundling full recipes instead of single items.Improve Retention through automated, predictive reminders that prevent users from switching to competitors when in a rush."3. The "Zero-Investment" Pitch StrategySince you want zero investment, emphasize "Platform Native" growth. In your application, mention:Zero UI Overhead: "The service lives entirely on WhatsApp and as an MCP-enabled agent, requiring no heavy frontend infrastructure."AWS Partnership: "I plan to leverage the Amazon Bedrock integration provided within the Builders Club to keep the LLM stack scalable and cost-effective."4. Execution Steps (Post-Application)StepActionTool (Free Tier)1BackendUse Supabase Edge Functions (TypeScript) to handle API logic.2InterfaceUse WhatsApp Cloud API (free for first 1k monthly conversations).3IntelligenceUse Amazon Bedrock (via Builders Club credits) or Gemini API free tier for parsing recipes.4DatabaseUse Supabase to store user "Order Cycles" (e.g., Milk = 3 days).The "Money" MomentOnce you have the API access, you don't need a marketing budget. You reach out to "Food Influencers" and say: "I built a tool where your followers can 'Order your Recipe' on Swiggy in one click. Want to put the link in your bio?"They get engagement; you get users. Zero investment, pure SaaS.